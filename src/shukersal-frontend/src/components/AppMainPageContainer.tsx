import { styled, Box } from '@mui/material';
export const AppMainPageContainer = styled(Box)({
    position: 'fixed',
    width: '100%',
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    //justifyContent: 'center',
    alignItems: 'center'
});
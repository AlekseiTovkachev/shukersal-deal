
import { Box, Button, Typography } from "@mui/material";

export const AppBarLoginButton = () => {
    return <Button
        color="secondary"
        variant="contained"
    >
        Login
    </Button>
};